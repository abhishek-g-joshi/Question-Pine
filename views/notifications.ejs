<%- include("partials/header.ejs") -%>

<div class="container">
    <header class="jumbotron">
      <h3 class="about-section"><b>Notifications</b></h3>

    </header>
</div>
<div class="">
  <%console.log(requestedDiscussionArray)%>
  <%for(let i=0; i<requestedDiscussionArray.length; i++){%>

    <form action="/storys/5" method="post">

      <!-- form fields here -->

      <p>Group Name : <%=requestedDiscussionArray[i].discussionName%></p>
      <p><%=requestedDiscussionArray[i].description%></p>
      <p>Group members:</p>
      <%const activeMembers = requestedDiscussionArray[i].currentMembers;%>
      <%for(let j=0; j<activeMembers.length; j++){%>

          <p><%=activeMembers[j]%></p>
      <%}%>

      <button type="submit" formaction="/discussion/<%=requestedDiscussionArray[i].discussionID%>/<%=user.userName%>/accept">Accept</button>
      <button type="submit" formaction="/discussion/<%=requestedDiscussionArray[i].discussionID%>/<%=user.userName%>/reject">Reject</button>

    </form>



  <%}%>

</div>


<%- include("partials/footer.ejs") -%>
